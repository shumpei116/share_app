<nav class="navbar navbar-default navbar-static-top nav-header">
  <div class="container-fluid navbar-inverse">
    <div class="navbar-header">
      <%= link_to "share app", root_url, class:"navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <div class="navbar-form navbar-left">
        <div class="form-group">
          <%= search_form_for @q, url: search_rooms_path do |f| %>
            <%= f.search_field :room_address_cont, placeholder: "\uf3c5  エリア", class:"fa form-control" %>
            <% f.submit "" %>
          <% end %>
        </div>
        <div class="form-group">
          <%= search_form_for @q, url: search_rooms_path do |f| %>
            <%= f.search_field :room_introduction_or_room_name_cont, placeholder: "\uf002  キーワード", class:"fa form-control" %>
            <% f.submit "" %>
          <% end %>
        </div>
      </div>
      
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="navbar-text"><%= current_user.name %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <% if current_user.image.attached? %>
              <%= image_tag(current_user.display_image(50,50),class:"img-circle", alt: "ユーザー画像") %>
            <% else %>
              <%= image_tag("profile.jpg", size: '40x40',class:"img-circle", alt: "ユーザー画像") %>
            <% end %>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "ルーム登録", new_room_path %></li>
              <li><%= link_to "予約済みルーム一覧", reservations_path %></li>
              <li><%= link_to "登録済みルーム一覧", posts_rooms_path %></li>
              <li><%= link_to "設定", users_account_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class=""><%= link_to "ログイン", user_session_path %></li>
          <li class=""><%= link_to "新規登録", new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>